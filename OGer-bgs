local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("🔥 OG Bubble Gum Simulator 🔥 - Made by onliox", "Sentinel")

local FarmingTab = Window:NewTab("Farming")
local BubbleSection = FarmingTab:NewSection("Bubble")

BubbleSection:NewToggle("Auto Blow", "ToggleInfo", function(state)
    if state then
        _G.autobubble = true;
        while _G.autobubble == true do
            game:GetService("ReplicatedStorage"):WaitForChild("Assets"):WaitForChild("Remotes"):WaitForChild("BlowBubble"):FireServer()
            wait()
        end
    else
        _G.autobubble = false;
    end
end)

BubbleSection:NewToggle("Auto Sell", "ToggleInfo", function(state)
    if state then
        _G.autosell = true;
        while _G.autosell == true do
            local args = {
                [1] = "TwilightSell"
            }
            
            game:GetService("ReplicatedStorage"):WaitForChild("Assets"):WaitForChild("Remotes"):WaitForChild("SellBubble"):FireServer(unpack(args))            
            wait()
        end
    else
        _G.autosell = false;
    end
end)

local Collector = FarmingTab:NewSection("Collect")

Collector:NewButton("ButtonText", "ButtonInfo", function()
    game:GetService("Players").LocalPlayer.PlayerGui.ScreenGui.ClientScript.Modules.GuiService.CashReward:Destroy()
end)

Collector:NewToggle("Collect Near", "ToggleInfo", function(state)
    if state then
        _G.collect = true;
while _G.collect == true do
local player = game.Players.LocalPlayer

local char = player.Character or player.CharacterAdded:Wait()
local root = char:WaitForChild("HumanoidRootPart")

for _, part in pairs(workspace.Pickups:GetChildren()) do
    if part:IsA("BasePart") then
        local distance = (root.Position - part.Position).Magnitude

        if distance <= 90 then
            root.CFrame = CFrame.new(part.Position + Vector3.new(0, 3, 0))
            task.wait(0.05)
        end
    end
end
    wait()
end
    else
        _G.collect = false;
    end
end)

Collector:NewToggle("Collect All (Laggy)", "ToggleInfo", function(state)
    if state then
        _G.collect1 = true;
while _G.collect1 == true do
for i, v in pairs(game:GetService("Workspace").Pickups:GetChildren()) do
firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, v, 0)
end
    wait(5)
end
    else
        _G.collect1 = false;
    end
end)

local HatchingTab = Window:NewTab("Hatching")
local DeleteSection = HatchingTab:NewSection("Delete Pets")
local EventEggs = HatchingTab:NewSection("Event Eggs")

DeleteSection:NewToggle("Auto Delete Non-Legendary", "ToggleInfo", function(state)
    if state then
        _G.deleteall = true;
while _G.deleteall == true do
    game:GetService("ReplicatedStorage"):WaitForChild("Assets"):WaitForChild("Remotes"):WaitForChild("PetUnlockedDelete"):FireServer()
    wait(5)
end
    else
        _G.deleteall = false;
    end
end)

EventEggs:NewToggle("Leprechaun Egg", "ToggleInfo", function(state)
    if state then
        _G.leprechaunegg = true;
while _G.leprechaunegg == true do
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-636, 42, 596)
for _,v in pairs(game:GetDescendants()) do
    if v.Name=="PurchaseEgg" then
        if v:IsA("RemoteEvent") then v:FireServer("Leprechaun Egg","Multi") else v:InvokeServer("Leprechaun Egg","Multi") end
        break
    end
end
    wait(0.1)
end
    else
        _G.leprechaunegg = false;
    end
end)
